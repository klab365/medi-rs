[package]
name = "medi-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0.64"

[dev-dependencies]
uuid = { version = "1.10.0", features = ["v4"] }
tokio = {version = "1.40.0", features = ["full"]}
rand = "0.8.5"

